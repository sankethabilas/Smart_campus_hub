spring:
  application:
    name: smart_campus_Operation-hub
  datasource:
    url: jdbc:mysql://localhost:3306/campus?createDatabaseIfNotExist=true
    username: root
    password: "Abi0021@"
  jpa:
    show-sql: true
  flyway:
    baseline-on-migrate: true 
